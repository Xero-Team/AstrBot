schema_version: 1

upstream:
  repo: https://github.com/AstrBotDevs/AstrBot.git
  branch: master

last_synced:
  commit: 69ae35a359e8c9a44375004e6b8252ee366102bb
  synced_at_utc: 2026-09-26T05:01:51Z
  sync_method: adapt
  source_pr: '10219, 10221, 10218, 10222'
  note: >-
    Reviewed 67c74b7ed8d5b8df170f849faaa431fc329c4ddc..69ae35a359e8c9a44375004e6b8252ee366102bb
    (4 commits). Adapted three: #10219 (preserve the user scroll offset while
    earlier WebChat history loads, mapped onto the fork Chat.vue
    shouldStickToBottom anchor logic and covered in chatViewSmoke.vitest.ts),
    #10221 (auto-scroll the reasoning activity panel, added to the fork
    ReasoningSidebar.vue with a chatPanels.vitest.ts regression), and #10218
    (send x-astrbot-conversation-id through the fork extra_headers_kwargs helper
    so custom provider headers survive; mapped the removed openai_source.py onto
    openai_chat_completions_source.py, threaded the ID through the standalone
    OpenAI Responses state/background/cancel helpers, and kept conditional
    query_kwargs in the Anthropic/OpenAI text paths; for Gemini, replaced the
    upstream provider-wide temporary-header mutation and its request lock with a
    per-request HttpOptions header on the GenerateContentConfig, so concurrent
    conversations are isolated without serializing streams; an httpx request hook
    reasserts the AstrBot user agent, which the per-request options would
    otherwise turn into a duplicate SDK User-Agent header). Skipped #10222 (desktop
    workspace directory picker) because its client-controlled custom project
    workspace path conflicts with the fork's server-derived
    astrbot/core/project_workspace.py roots and the fork project API has no
    workspace_type/workspace_path fields; revisit if the fork adopts backend
    project workspace types. No upstream version change in this interval, so no
    version or changelog update. Focused pytest and dashboard vitest checks
    passed.
