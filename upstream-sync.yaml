schema_version: 1

upstream:
  repo: https://github.com/AstrBotDevs/AstrBot.git
  branch: master

last_synced:
  commit: 7bad30ebe40aae21fb41e7ba8e4f9b6d5b8e6f8a
  synced_at_utc: 2026-08-17T13:21:12Z
  sync_method: manual-adaptation
  source_pr: ''
  note: >-
    Merged the Xero-Team/AstrBot sibling fork (remote `upstream`, tip
    7bad30ebe) into Zephirum via a merge commit, bringing in 9 commits
    diverged since the previous marker fa399cb09. These are the sibling
    fork's own commits (no AstrBotDevs PR numbers), so source_pr is empty.
    Major upstream changes: completion of the dashboard Vuetify 4
    migration; gating sensitive dashboard mutations behind step-up auth
    with new configStepUp/botStepUp/providerStepUp utilities and tests;
    aligning bot step-up resource identity; preserving dashboard step-up
    config scope; and responsive dashboard layout / a11y test coverage.
    The merge had zero textual conflicts (git auto-merged all 60 files).
    Semantic review: upstream added `_resource_scoped_dashboard_context`
    at the `authorize()` entry, but it only rewrites `source == dashboard`
    contexts, so IM-sourced BTW work-loop contexts pass through unchanged
    and the fork's `btw_work_elevation` + `btw_elevated_actions` elevation
    path is intact. Upstream's new step-up tests coexist with the fork's
    per-action elevation test. The fork's per-profile work-loop permission
    matrix (committed as 45e54538d just before this merge) was preserved
    unchanged; Vuetify 4 migration did not affect the
    `BtwElevatedActionsSelector` component (standard v-table/v-switch/
    v-tooltip). Validation: pytest 2278 passed / 5 skipped on the full
    unit suite, dashboard vitest 159 passed, vue-tsc and ruff clean. Two
    Windows+PowerShell-only shell tests in test_computer.py
    (TestLocalShellComponent::test_exec_with_cwd / test_exec_with_env)
    fail on this local Windows host due to a pre-existing test-helper
    quoting mismatch (cmd-style list2cmdline vs the pwsh -Command
    wrapping added by an earlier upstream batch); this batch did not
    touch local.py or test_computer.py, the failures are unchanged from
    the prior merge, do not run in CI (ubuntu-latest), and are unrelated
    to BTW. The `upstream.repo` field continues to point at
    AstrBotDevs/AstrBot per fork convention even though this sync pulled
    from Xero-Team/AstrBot; Zephirum operates as an independent fork and
    does not open PRs back to either upstream.
