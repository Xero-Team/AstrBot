schema_version: 1

upstream:
  repo: https://github.com/AstrBotDevs/AstrBot.git
  branch: master

last_synced:
  commit: 15fe5213521ca7cea5d47336c7a6fbe47895c800
  synced_at_utc: 2026-08-25T00:00:00Z
  sync_method: merge
  source_pr: ''
  note: >-
    Merged upstream/master (Xero-Team/AstrBot) tip 15fe52135 into Zephirum
    `master` via a merge commit, advancing from the previous marker
    ceb53f8c5. Sixteen sibling-fork commits in this interval add: a grouped
    `/bot` command (status/enable/disable/leave) with stable command_ids and
    `session.read`/`session.manage` permissions; a `SessionStatusCheckStage`
    passthrough so activated `/bot status` and `/bot enable` run while the
    session is off; Slack non-image file-download authentication; the
    Anthropic httpx/httpx2 module rename; per-knowledge-base BM25 fallback
    index caching; shared-preference and live-TTS write-queue bounding;
    removal of the unsupported Kubernetes manifests and `privileged` Compose
    mode; pinning Hypercorn and dropping unused deps; the conversation-detail
    edit-mode double-scrollbar fix; and bounding hung dynamic imports in
    dashboard coverage tests. The fork's community-files PR (f0247df01) was
    merged upstream as e10794d5c; upstream follow-ups 7c426dd0d (route support
    to GitHub, label Discord/QQ and docs.astrbot.app as upstream) and
    06b9ef197 (reject discriminatory remarks targeting AI) were taken.
    CODE_OF_CONDUCT.md auto-merged: the fork's trimmed pledge (no
    identity-attribute listing) is preserved, and the new AI-discrimination
    clause is about AI contributions, not a protected-attribute diversity
    listing, so it is accepted. Five files conflicted and were resolved per
    the protect-BTW policy: `.github/ISSUE_TEMPLATE/config.yml` and
    `SUPPORT.md` took upstream's fork-aware routing; `CHANGELOG.md` took
    upstream's index (adds v4.27.5 as WIP); `dashboard/tests/finalPush.vitest.ts`
    kept the fork's 60s `timeout` (CI fix upstream lacks); `upstream-sync.yaml`
    kept the fork's tracking record. No BTW-core files conflicted: the `/bot`
    command and `session_status_check/stage.py` are additive and were adopted
    as-is, and the fork's `btw_work_elevation`, `btw_detached_work`,
    `btw_agent_lock_key`, per-profile `btw_elevated_actions`, and dual-loop
    agent/conversation machinery are untouched. The `upstream.repo` field
    continues to point at AstrBotDevs/AstrBot per fork convention even though
    this sync pulled from Xero-Team/AstrBot; Zephirum operates as an
    independent fork and does not open PRs back to either upstream.
    Validation: ruff check + ruff format --check both pass; 272 focused
    BTW/auth/pipeline/bot/scheduler tests pass; 148 new upstream unit tests
    (slack, anthropic, kb sparse retriever, shared_preferences, container
    contracts, agent_run_util) pass; 24 affected dashboard vitest cases
    (finalPush, coverageMounts, viewDialogLayouts) pass; full unit suite 4237
    passed / 5 skipped. The 12 failures are exactly the pre-existing
    Windows-only shell/fs/CRLF/drive-letter cases (TestLocalShellComponent,
    TestSyncSkillsToSandbox, test_local_shell_component, test_computer_fs_tools,
    test_sanitize_upload_filename) documented at the previous marker; the merge
    did not modify those test files or their sources (0-line diff vs pre-merge
    master), and they pass in CI on ubuntu-latest.
