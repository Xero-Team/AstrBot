interface:
  display_name: 'Create AstrBot Plugin'
  short_description: 'Build and verify plugins using the public AstrBot SDK'
  default_prompt: 'Use $create-astrbot-plugin to implement the requested plugin with the current public SDK, document its usage, and run the relevant checks.'
